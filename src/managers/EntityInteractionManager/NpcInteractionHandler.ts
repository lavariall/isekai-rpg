

/**
 * Handles interactions with NPCs.
 */
export class NpcInteractionHandler {
    /**
     * Handles a dialog interaction.
     * @param npcKey The key of the NPC.
     */
    public static talk(npcKey: string): void {
        console.log(`Interacting with NPC: ${npcKey}`);
        // Expand with dialog system logic later
    }
}
